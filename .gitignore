# ============================================================================
# Emergent platform machinery (not part of the tunemavens app source)
# ============================================================================
.emergent/
.ruff_cache/
test_reports/
tests/
memory/
test_result.md
yarn.lock

# Nested repo clones (development scratch — never push these)
intermaven_repo/
tunemavens_repo/

# Old emergent CRA scaffold (kept locally only so supervisor can delegate to vite)
frontend/
backend.OLD/

# ============================================================================
# Standard ignores
# ============================================================================
# IDE
.idea/
.vscode/

# Node
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Builds
dist/
dist-ssr/
build/
/coverage
*.local

# Python (backend)
__pycache__/
*.py[cod]
*.so
.Python
.venv/
venv/
*.egg-info/

# Backend env (committed: .env.example only)
backend/.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
